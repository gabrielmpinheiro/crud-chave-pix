version: '3.3'

services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'crudpix'
      MYSQL_USER: 'usermaster'
      MYSQL_PASSWORD: '12qwaszx'
      MYSQL_ROOT_PASSWORD: '12qwaszx'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - my-db:/var/lib/mysql
